<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mongodb CRUD operation using ejs</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

    <div class="w-full h-full p-10 bg-zinc-900 text-white">
        <div class="nav mb-5 flex items-center justify-end">
            <a class="text-base text-blue-500" href="/">Back to Home</a>
        </div>
        <h1 class="text-xl mb-5">All User:</h1>
        <div class="users flex gap-5 flex-wrap">
            <% if(users.length > 0 ){ %>
                <% users.forEach(function(user){ %>
                    <div class="user w-64 p-4 bg-zinc-800 rounded-lg">
                        <div class="w-full h-52 bg-zinc-500 rounded-lg overflow-hidden ">
                            <img class="w-full h-full object-cover object-top" src="<%=user.image %>" alt="">
                        </div>
                        <h3 class="text-xl tracking-tighter mt-3 "><%= user.name %></h3>
                        <h5 class="text-base tracking-tighter text-zinc-400 "><%= user.email %></h5>
                        <div class="flex items-center justify-between mt-3">
                            <a class="text-base tracking-tighter text-blue-400 " href="/edit/<%= user._id %>">Edit User</a>
                            <a class="text-base tracking-tighter text-red-400 " href="/delete/<%= user._id %>">Delete User</a>
                        </div>
                    </div> 
                <% }) %>
            <% }else{ %>
                <h3 class="text-zinc-500">No Users Yet</h3>    
            <% } %>
        </div>
    </div>
</body>
</html>